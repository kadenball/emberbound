# Local validation — 0.16.0, 2026-09-08

- **124 browser checks passed**, eight inapplicable combinations skipped, across desktop Chromium, Android-sized Chromium and iPhone-sized WebKit. This includes 24 new controller/menu combinations and the existing gameplay, touch, local co-op, bookmark, boss-counter, story, ending and audio checks. Host-requirement validation was skipped by environment flag; actual browser execution completed.
- **151 simulation tests passed** across 16 files. Combat balance was not changed; the 432-run [0.14 strategy audit](COMBAT-AUDIT-0.14.json) remains the latest balance evidence.
- New browser checks cover once-per-press story progression, story Back/cancel, focus across hero redraws, offscreen focus scrolling, simultaneous/held surgery confirms, styles and weapon persistence, settings and pause return, solo disconnect/reconnect, keyboard Tab/Esc/P, held keyboard/gamepad/analog combat barriers, and bookmark-preserving defeat retry. The forced-defeat fixture invokes the production damage path to test the menu and retry; it is not difficulty evidence.
- A real-input boss simulation driver reaches entrance, rage and defeat while controller presses operate each performance, aftermath dialogue, rewards and the next chapter. Existing natural-defeat/retry checks and P2 controller ownership checks also pass.
- The iPhone-sized hero focus screenshot was visually inspected. Stitched focus is visible and scrolls into view; the hint fits below the card. Captures: `artifacts/controller-heroes-{desktop,android,iphone}.png`.
- Strict TypeScript, production Vite build and Capacitor Android/iOS sync passed. Main JavaScript: **316.62 kB / 114.33 kB gzip**; CSS: **53.81 kB / 12.24 kB gzip**. Rollup removes misplaced pure-annotation comments in the Zod dependency; the build succeeds.
- Android `assembleDebug bundleRelease lintRelease` passed on the final assets, **version 0.16.0/code 16**, independently confirmed from the APK manifest. The debug APK is installable; the release AAB remains unsigned. The existing dependency lint baseline remains.
- Xcode project syntax and two iOS plists parse. Native iOS compilation, signing and device behavior remain unverified on this Linux host.
- Packaging compares every current web/audio asset in both native packages before collecting downloads and generating source checksums. `artifacts/SHA256SUMS.txt` accompanies the APK, unsigned AAB and source archive.

This pass adds adventure-loop controller usability, not proof of reference-game quality. Human play/listening review, journey pacing, remaining combat exploits, physical controller and device lifecycle/performance, and native iOS release work remain open. See [quality target](QUALITY-TARGET.md), [playtest kit](PLAYTEST.md), [0.16 changes](UPDATE-0.16.md), and [previous validation](VALIDATION-0.15.md).
