# Local validation — 0.24.0, 2026-09-08

- **201 simulation tests passed** across 22 files, including the nine earned-progression hero/style campaigns. Combat, levels and progression rules are unchanged from 0.23; the 432-run combat audit was not rerun.
- The real installed debug APK passed **eight native smoke checks** on the dedicated Android 16/API 36 emulator: landscape startup, menu transport, hero/music preferences in Android SharedPreferences, offline Jump/Power touches, Android Back, quick Home/return with an explicit pause, complete profile persistence across process death, and saved-hero recovery without duplicate banked rewards. No WebView JavaScript errors were reported. This is a first-chapter smoke test, not a native campaign or physical-phone test. [Raw result](ANDROID-SMOKE-0.24.0.json), [reproduction](../scripts/native/README.md).
- The native baseline reproduced quick Home/return continuing combat. The new Capacitor `pause` listener handles Android `onPause` before the later inactive-state event. Native parent/window background painting also removes the white strip around the reserved display-cutout area. Home and resumed captures were inspected. The foreground-pause screenshot catches an OS transition; its visual settling is not asserted.
- An initial concurrent browser/build/emulator run exhausted available host memory and was stopped: five checks passed, four failed, three were interrupted and three did not run. After shutting down the emulator, the unchanged **15-check sequential browser selection passed 14** (hero/settings persistence, battle/pause/camp, layout, controller-menu behavior and Chromium mixer checks). iPhone-sized WebKit's mixer signal check intermittently returned zero; three isolated repetitions passed twice and failed once. It remains an open audio-reliability finding, not an all-green run. The failure is in a standalone mixer fixture that does not import the changed native lifecycle handler. Six diagnostic contexts and six fresh-browser probes all showed ready media, an advancing running audio context and nonzero samples; these do not erase the failing original test. [Diagnostic samples](WEBKIT-AUDIO-PROBE-0.24.json).
- Strict TypeScript, production Vite build and Android/iOS Capacitor sync passed. Main JavaScript is **351.03 kB / 127.80 kB gzip**; CSS **54.32 kB / 12.38 kB gzip**.
- Android `assembleDebug bundleRelease lintRelease` passed. APK manifest confirms **0.24.0 / code 24**. Native packages contain the current web assets; packaging verifies their bytes and writes checksums. Release AAB remains unsigned.
- Xcode project and both iOS plists parse; native iOS build, signing and device behavior remain unverified on Linux.

This pass fixes interruption and native-window behavior. Human fun, pacing, combo value, remaining combat exploits, audible music/warning quality, physical-device performance and native iOS release quality remain open. The previous Count Snackula pacing observation and 0.23 strategy results still apply. See [changes](UPDATE-0.24.md), [previous validation](VALIDATION-0.23.md), [quality target](QUALITY-TARGET.md) and the [Super Meat Boy / Castle Crashers comparison](LEVEL-DESIGN-RESEARCH.md).
