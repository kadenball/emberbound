# Local validation — 0.21.0, 2026-09-08

- **184 simulation tests passed** across 20 files. New scene-selection checks follow an actual boss approach and the entrance/rage/defeat performance state machine, including the handoff to the won state without a second victory transition. Existing earned-progression campaigns, co-op and supply checks remain green.
- **146 browser checks passed**, with ten inapplicable combinations skipped, across desktop Chromium, Android-sized Chromium and iPhone-sized WebKit. Host-requirement validation was skipped by environment flag; actual browser execution completed.
- New isolated mixer tests measure a nonzero signal from the production WebAudio graph, verify near-silence on mute/pause, independent music/effects toggles, cue position across pause/resume, no replay on repeated scene updates, no stale cue after Music is re-enabled, reserved warning voices, duplicate suppression and score duck/recovery. They run in all three browser configurations. An initial WebKit failure reproduced silent, stalled preloaded menu media; assigning the file after media-source routing fixed the reproduction and the final suite passes.
- The existing actual-input first-boss browser flow now also checks entrance/rage/victory cue selection and playback through the real UI. The chapter score remains active through the aftermath and reward screen. Final-king story, reward and persistence flows still pass. Mixer fixtures and the staged recording are separately identified; they are not human play sessions.
- All **18 regional cue files** decode as stereo and match their packaged SHA256 values, with distinct hashes. Separate ffmpeg decoding found no clipped samples; cue sample peaks range from **−6.42 to −2.07 dBFS**. These are sample peaks, not intersample true-peak measurements.
- The **14.16-second staged mix recording** uses the actual production mixer for combat → kettle entrance → impact/warning burst → rage → victory. Its decoded MP3 sample peak is **−3.24 dBFS**, RMS **−24.59 dBFS**, with zero clipped samples. It is available for listening at `artifacts/audio-review-0.21.mp3`; no human listening quality or perceptual warning-clarity claim follows from those measurements. See [raw audio audit](AUDIO-AUDIT-0.21.json).
- Strict TypeScript, production Vite build and Capacitor Android/iOS sync passed. Main JavaScript: **347.70 kB / 126.45 kB gzip**; CSS: **53.81 kB / 12.24 kB gzip**. New cue assets occupy about 1.3 MiB on disk. The existing twelve chapter recordings were not regenerated.
- Android `assembleDebug bundleRelease lintRelease` passed with the final assets. Version **0.21.0/code 21** was independently confirmed from the APK manifest. The debug APK is installable; the release AAB remains unsigned. The existing dependency lint baseline remains.
- Xcode project syntax and both iOS plists parse. Native iOS compilation, signing, audio interruption and physical-device behavior remain unverified on this Linux host.
- Packaging compares every current web/audio asset in both native packages before collecting the APK, unsigned AAB and source archive and generating checksums.

This pass changes no combat, progression or map rules. The **0.20** matched combat audit (432 runs) and supply audit (162 runs) remain the latest such evidence and were **not rerun** for the audio change: Heavy-only wins 30/108, including 19/54 boss chapters; mixed wins 104/108, including 53/54 boss chapters. The supply waiting/skipping probes each win 53/54 journeys without supply damage. Those scripts do not demonstrate human enjoyment. Full details remain in [0.20 validation](VALIDATION-0.20.md).

The goal remains open. Human pacing/comedy/enjoyment, broader combat exploits, musical quality and warning audibility, physical-device/controller/lifecycle performance and native iOS release verification remain outstanding. See the [quality target](QUALITY-TARGET.md), [playtest kit](PLAYTEST.md), [0.21 changes](UPDATE-0.21.md) and [reference comparison](LEVEL-DESIGN-RESEARCH.md).
