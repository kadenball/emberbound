# Local validation — 0.25.0, 2026-09-08

- **204 simulation tests passed** across 22 files, including all nine earned-progression hero/style campaigns. The Count checks cover normal/rage timings, a gap between low and overhead hazards, fixed targets while the player moves, recovery after attack resolution, reversed sweep order, grounded armor rejection and a real aerial head strike. The first test draft assumed a fixed wait that was too short for the longer rage volley; the test now advances until its sweets resolve.
- **18 boss browser checks passed** across desktop Chromium, Android-sized Chromium and iPhone-sized WebKit. The Count's extended four-capture sequence then passed on all three layouts, and passed again after delaying its recovery capture until the armor finished unfolding. No product code changed for that capture timing. The normal offensive driver is restored; it no longer suppresses attacks to wait for dentures. Phone-sized sweets, jaw warning and opened-body captures were inspected.
- The final **432-run matched combat audit** retains 0.23's profiles, seeds and four policies: Light **0/108**, Heavy **25/108** (bosses **13/54**), Light + power **22/108** (bosses **15/54**), mixed **103/108** (bosses **51/54**). All terminate. The six changed wins are Light + power against the Count; mixed beats him 9/9 before and after. The older supply-choice audit was not rerun. [Raw combat evidence](COMBAT-AUDIT-0.25.json).
- Strict TypeScript, Vite production build and Android/iOS Capacitor sync passed. Main JavaScript **351.75 kB / 128.07 kB gzip**; CSS **54.32 kB / 12.38 kB gzip**. Audio sources and mixer behavior are unchanged.
- Android `assembleDebug bundleRelease lintRelease` passed with current assets. Manifest version **0.25.0 / code 25**. Packaging compares every current web/audio asset against both native packages and generates artifact checksums. The debug APK is installable; the release AAB remains unsigned.
- Xcode project syntax and both iOS plists parse. Native iOS compilation, signing and physical-device behavior remain unverified on Linux.
- Native lifecycle/storage evidence remains the **0.24 API 36 emulator smoke**, not a new 0.25 native gameplay run. The native background and pause code are unchanged. The broader browser suite was not rerun for this boss change; 0.24's intermittent WebKit mixer zero-signal finding remains unresolved and is not erased by passing boss-render tests.

The goal remains open. This pass deepens one encounter's defense sequence and performance without increasing its health. Human fun, pattern discoverability, touch comfort, broader journey pacing, remaining strategy exploits, audio quality/reliability, sustained co-op and native release readiness remain unproven. [Changes](UPDATE-0.25.md), [quality target](QUALITY-TARGET.md), [reference comparison](LEVEL-DESIGN-RESEARCH.md), [previous validation](VALIDATION-0.24.md).
