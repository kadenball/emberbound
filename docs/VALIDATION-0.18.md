# Local validation — 0.18.0, 2026-09-08

- **130 browser checks passed**, with eight inapplicable combinations skipped, in the full final-code run across desktop Chromium, Android-sized Chromium and iPhone-sized WebKit. Host-requirement validation was skipped by environment flag; actual browser execution completed. The suite covers combat, boss defenses, sabotage, support-enemy warnings, retries/bookmarks, progression, controllers, story and audio integration.
- All six staged journey contact sheets were visually reviewed. Updated laundry and royal sheets and final phone-sized woodland/laundry battle and royal aftermath fixtures were also inspected. These deliberately positioned fixtures establish visual evidence, not human enjoyment or actual-play results. The separate browser suite exercises real inputs and production gameplay paths.
- The journey capture script reported no page errors. Warm Canvas submission medians ranged from **0.5–1.0 ms**, with p95 values of **0.6–3.0 ms**. Camera-sweep frames that built terrain chunks had regional medians of **6.5–15.7 ms** and a maximum of **23.3 ms**; at most three terrain chunks were retained. This final capture ran concurrently with browser checks and the build, so host contention affects timings. These are headless desktop measurements, not native phone frame rates, GPU timings or thermal evidence. Raw data: `artifacts/journey-render-timings.json`.
- Strict TypeScript, the final production Vite build and Capacitor Android/iOS sync passed. Main JavaScript: **335.91 kB / 122.17 kB gzip**; CSS: **53.81 kB / 12.24 kB gzip**.
- Android `assembleDebug bundleRelease lintRelease` passed. Version **0.18.0/code 18** was independently confirmed from the APK manifest. The debug APK is installable; the release AAB remains unsigned. The existing dependency lint baseline remains.
- Xcode project syntax and both iOS plists parse. Native iOS compilation, signing and device behavior remain unverified on this Linux host.
- Combat rules were unchanged in this presentation pass. The latest **158 simulation tests** and **432-run combat audit** are from 0.17 and were **not rerun for 0.18**. In that audit, Heavy-only still won 31/108 runs, including 17/54 boss runs; the broader exploit remains unresolved. See [previous validation](VALIDATION-0.17.md) for conditions and the other policies.
- Packaging compares every current web/audio asset in both native packages before collecting the APK, unsigned AAB and source archive and generating their checksums.

The goal remains open. Thirty distinct journey landmarks, weathering and cleared-place aftermaths address world identity and floor readability. Encounter spacing and travel distances are unchanged. Human wayfinding, travel pacing, combat exploits, listening review, physical controllers/device lifecycle/performance and native iOS release verification remain outstanding. See the [reference comparison](LEVEL-DESIGN-RESEARCH.md), [quality target](QUALITY-TARGET.md), [playtest kit](PLAYTEST.md) and [0.18 changes](UPDATE-0.18.md).
