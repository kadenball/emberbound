# Local validation — 0.12.0, 2026-09-08

- **131 simulation tests passed** in 14 files. Ten new bookmark tests cover trailhead/cleared-road restoration, equality with an in-memory fight retry, co-op continuation, restored boss victory, completed sabotage, invalid data, method-field stripping, practice isolation and ordered storage/settlement writes. Existing campaign probes validate deserialization at every entry and cleared-road checkpoint across all nine hero/style earned-progression combinations.
- One sabotage regression was adjusted to fail during the active disaster. A successfully cleared sabotage now creates a new road checkpoint and correctly preserves its wreck and rewards; separate new coverage checks that behavior.
- **82 browser checks passed**, eight inapplicable combinations skipped, across desktop Chromium, Android-sized Chromium and iPhone-sized WebKit. The full run passed 76 checks; six combinations in two older tests still assumed the former direct-start flow after reload. Those tests were updated to choose a new adventure through the bookmark replacement screen, preserving their style/story assertions, and all six rechecks passed. Production code did not change between that full run and the focused rechecks.
- New browser coverage reloads during a later real encounter, restores entry resources and the saved hero/loadout, saves and quits, retains co-op, handles explicit replacement/abandonment, leaves a failed write paused, and blocks overwriting the profile after a failed read. The later-fight setup uses accelerated normal inputs without HP/position/wave edits; the actual resume and storage UI is exercised.
- The final king test drives entrance, rage and defeat, reads all five ending pages, verifies the crown weapon and six teeth, and reloads into durable completion without a stale bookmark or duplicate payout. Its plausible late-campaign profile is seeded; this is not a fresh-profile human campaign playthrough.
- The iPhone-sized bookmark home/pause and final reward captures were visually inspected. The landscape reward layout was revised so its loot and primary action fit together; browser bounds assertions cover both. Capture paths are in `UPDATE-0.12.md`.
- Strict TypeScript, production Vite build and Capacitor Android/iOS sync passed. Zod 4.5.4 is pinned for runtime bookmark validation; its MIT license is bundled. The main production script is 291.68 kB / 104.27 kB gzip.
- Android `assembleDebug bundleRelease lintRelease` passed, **version 0.12.0/code 12**. The debug APK is installable; the release AAB remains unsigned. The existing dependency lint baseline remains.
- Xcode project syntax and two iOS plists parse. Native iOS build/signing has not been verified on this Linux host.
- Packaging verifies every web/audio asset in both native packages against the current production build, then writes source/download checksums.

The full quality target remains open. Persistent bookmarks and final-ending UI are now covered in browsers, but physical-device process termination, storage pressure, native frame pacing/lifecycle, native iOS release work, gamepad menu navigation, human music review and sustained enjoyment remain outstanding. See [the open target](QUALITY-TARGET.md), [0.12 changes](UPDATE-0.12.md), [playtest kit](PLAYTEST.md) and [prior validation](VALIDATION-0.11.md).
