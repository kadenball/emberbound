# Local validation — 0.23.0, 2026-09-08

- **201 simulation tests passed** across 22 files, including all nine earned-progression hero/style campaigns. Twelve new move-recipe checks cover the eight weapons' shared finishers, connected-Light input setup, buffered direction, expired chains, royal brush contact behavior and the level-eight counter quake. After bounding a test's hit-stop wait, all twelve recipe tests passed again.
- The full browser run passed **149 checks**, skipped ten inapplicable combinations and failed the three Count Snackula jaw-warning captures. The fixture's player won before that second pattern appeared. The observation driver now withholds offense once that boss appears until the warning/impact are observed; it still uses normal defensive inputs and does not edit health, position, phases or waves. **All twelve boss-counter checks then passed**. No product code changed after the full run. These runs collectively cover the 152 applicable checks; they are not represented as one uninterrupted all-green run.
- The new three-platform chain fixture supplies a healthy isolated target, earns three hits through normal Light inputs, pauses for capture, then performs the directional quake. Android-sized Chromium uses simultaneous joystick/Heavy touches; desktop Chromium and iPhone-sized WebKit use keyboard inputs. This is not native iPhone two-thumb evidence. The existing production gameplay, rewards, story, bookmark, controller, co-op and audio transport/signal checks passed in the full run.
- Android- and iPhone-sized ready-state captures were inspected. The three dots, remaining-time bar, ordinary spin hint and directional quake alternative are visible. Captures: `artifacts/chain-{ready,quake}-{desktop,android,iphone}.png`. They show an isolated input fixture, not an uninterrupted human play session. P2's HUD receives an offset while the new chain display is visible; human two-person readability remains unverified.
- The final **432-run matched combat audit** retains 0.22's profiles, seeds and all four input policies. Light wins **0/108**; Heavy **25/108**, including **13/54** boss chapters; Light + power **28/108**, including **21/54** boss chapters; mixed **103/108**, including **51/54** boss chapters. All terminate. Previous mixed totals were 104/108 and 53/54; individual outcomes move in both directions. See [raw audit](COMBAT-AUDIT-0.23.json). The earlier 162-run supply-choice audit was not rerun with these combat changes.
- Strict TypeScript, production Vite build and Capacitor Android/iOS sync passed. Main JavaScript: **350.96 kB / 127.78 kB gzip**; CSS: **54.32 kB / 12.38 kB gzip**. Audio files and mixer rules are unchanged from 0.21.
- Android `assembleDebug bundleRelease lintRelease` passed with the final assets. Version **0.23.0/code 23** was independently confirmed from the APK manifest. The debug APK is installable; the release AAB remains unsigned. The existing dependency lint baseline remains.
- Xcode project syntax and both iOS plists parse. Native iOS compilation, signing and physical-device behavior remain unverified on this Linux host.
- Packaging compares every current web/audio asset in both native packages before collecting the APK, unsigned AAB and source archive and generating checksums.

The goal remains open. This pass preserves learned moves, separates the quake input and corrects inconsistent timing/contact feedback. It does not establish reference-game quality. Human directional-input comfort, combo value, pacing/comedy/enjoyment, remaining strategy exploits, musical quality and warning audibility, physical-device/controller/lifecycle performance and native iOS release verification remain outstanding. Count Snackula dying before his second volley in the visual probe is an additional pacing observation. See the [quality target](QUALITY-TARGET.md), [playtest kit](PLAYTEST.md), [0.23 changes](UPDATE-0.23.md), [reference comparison](LEVEL-DESIGN-RESEARCH.md) and [previous validation](VALIDATION-0.22.md).
