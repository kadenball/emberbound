# Local validation — 0.15.0, 2026-09-08

- **100 browser checks passed**, eight inapplicable combinations skipped, across desktop Chromium, Android-sized Chromium and iPhone-sized WebKit. After that full run, the background was compressed to align the floor with the upper playable lane and the rectangle helper's no-outline case was corrected; all **18 boss-counter/defense browser checks passed again** on the final presentation. Host-requirement validation was skipped by environment flag; actual browser execution completed.
- The browser coverage includes real-input first-boss and final-ending flows, all new high/low hazard types, blocked attacks followed by successful head/rear counters, map navigation, bookmarks, local co-op and audio transport. No gameplay/balance rule changed in this visual pass. The 151 simulation tests and 432-run balance audit from [0.14](VALIDATION-0.14.md) were not repeated; they remain the latest simulation/balance evidence.
- The six-room roster, six full renderer fixtures and six actual boss map-preview images were regenerated on the final drawing code. The roster, phone Candy warning and full laundry fixture were visually inspected. Review exposed and fixed a half-cut journey landmark, an old foreground strip and a floor edge below the movement bounds. Captures are listed in [0.15](UPDATE-0.15.md).
- The final staged desktop Chromium benchmark recorded **0.4–0.7 ms median** and **0.7–1.2 ms p95** cached Canvas draw submission time across six rooms, retaining two terrain chunks each, with no page errors. These measurements do not include GPU completion, device thermals, native frame pacing or human play. Raw measurements: `artifacts/arena-render-timings.json`.
- Strict TypeScript, production Vite build and Capacitor Android/iOS sync passed. Main production JavaScript: 308.97 kB / 111.70 kB gzip. No downloaded texture pack or additional full-level texture cache was introduced. Rollup removes misplaced pure-annotation comments in the Zod dependency; the build succeeds.
- Android `assembleDebug bundleRelease lintRelease` passed on the final assets, **version 0.15.0/code 15**. The debug APK is installable; the release AAB remains unsigned. The existing dependency lint baseline remains.
- Xcode project syntax and two iOS plists parse. Native iOS build/signing has not been verified on this Linux host.
- Packaging compares every web/audio asset in both native packages with the current production build before collecting downloads and source checksums.

The full goal remains open. The new environments have stronger visible identities, but this is not proof of reference-game quality. Human atmosphere, music and play review, journey pacing, remaining combat exploits, physical-device performance/lifecycle, native iOS release work and gamepad menu navigation still need work. See [quality target](QUALITY-TARGET.md), [playtest kit](PLAYTEST.md), [0.15 changes](UPDATE-0.15.md) and [previous validation](VALIDATION-0.14.md).
