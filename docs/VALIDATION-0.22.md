# Local validation — 0.22.0, 2026-09-08

- **189 simulation tests passed** across 21 files, including all nine earned-progression hero/style campaigns and the existing co-op coverage. Five new royal-protection checks exercise ordinary-hit rejection, clearance of both guards, body collision, a player-input Pan Heavy counter, and stale/non-summoned staff. The tests distinguish isolated setup from actual-input campaign runs.
- **149 browser checks passed**, with ten inapplicable combinations skipped, across desktop Chromium, Android-sized Chromium and iPhone-sized WebKit. Host-requirement validation was skipped by environment flag; actual browser execution completed.
- The new browser scenario plays the final chapter with normal production combat inputs, pauses at the king's first protected state for capture, continues those inputs and observes staff clearance and the opening cue. It does not change HP, positions, enemies, waves or rewards. All three platform configurations pass. The existing final-king rage, defeat, dialogue, crown weapon, reward and reload flows also pass.
- Phone-sized protected and exposed captures were inspected. Receipt links connect the staff to the crown; the king raises his stamped papers while protected and drops them when the seal breaks. The opening has a floor ring and consistent objective/boss cues. These captures are not human readability or physical-device performance evidence. Files: `artifacts/royal-{protected,exposed}-{desktop,android,iphone}.png`.
- The **432-run matched combat audit** reuses the exact 0.20 earned profiles, seeds and steering policies. Light wins **0/108**; Heavy **25/108**, including **14/54** boss chapters; Light + power **28/108**, including **21/54** boss chapters; mixed **104/108**, including **53/54** boss chapters. All runs terminate. The five changed outcomes are Heavy-only final-king runs, now **0/9** versus **5/9** before. Mixed final-king remains **9/9**. See [raw audit](COMBAT-AUDIT-0.22.json).
- The mixed final-king chapter median rises **32.7 → 43.4 seconds** under that same probe. Those timings include approach fights and scripted performance handling, and are not human play durations. More fighting does not itself prove better pacing or fun. The older **162-run supply audit** was not rerun: this pass changes no supply rules.
- Strict TypeScript, production Vite build and Capacitor Android/iOS sync passed. Main JavaScript: **349.95 kB / 127.23 kB gzip**; CSS: **53.81 kB / 12.24 kB gzip**. Audio files and mixer rules are unchanged from 0.21; their browser signal/transport checks pass in the full suite.
- Android `assembleDebug bundleRelease lintRelease` passed with the final assets. Version **0.22.0/code 22** was independently confirmed from the APK manifest. The debug APK is installable; the release AAB remains unsigned. The existing dependency lint baseline remains.
- Xcode project syntax and both iOS plists parse. Native iOS compilation, signing and physical-device behavior remain unverified on this Linux host.
- Packaging compares every current web/audio asset in both native packages before collecting the APK, unsigned AAB and source archive and generating checksums.

The goal remains open. This pass gives the king a target-choice mechanic and a stronger bowling payoff; it does not establish reference-game quality. Human pacing/comedy/enjoyment, broader strategy exploits, musical quality and warning audibility, physical-device/controller/lifecycle performance and native iOS release verification remain outstanding. See the [quality target](QUALITY-TARGET.md), [playtest kit](PLAYTEST.md), [0.22 changes](UPDATE-0.22.md), [reference comparison](LEVEL-DESIGN-RESEARCH.md) and [previous validation](VALIDATION-0.21.md).
