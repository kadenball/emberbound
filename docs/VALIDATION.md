# Local validation — 0.31.0, 2026-09-08

- **All ten user-provided Udio links downloaded successfully** as distinct playable MP3s, approximately 130.86 seconds each. The originals total 29.97 MiB and remain in `music/source/udio/`. All ten were decoded and mastered by FFmpeg. Twelve chapter assignments share ten bundled recordings; 24 entrance/rage/victory/defeat excerpts have recorded source offsets. Every packaged file matches its manifest hash. The main-menu MP3 retains its exact pre-change SHA256. [Sources and processing](../public/audio/SOURCE.md), [assignments](UPDATE-0.31.md).
- **244 simulation tests pass** across 27 files after the music change, including the new Heavy counter/jump escapes, blood bounds, eighteen additional districts, late-entry checkpoint machinery and all nine earned-progression hero/style campaigns. Longer solo and co-op campaign probes allow bounded entry retries; these are real retry operations, not health edits. [0.30 gameplay evidence and strategy audits](UPDATE-0.30.md).
- **24 applicable music/browser checks pass**, with six platform-specific skips, in 44.4 seconds against the final 0.31 soundtrack. Desktop Chromium decodes every assigned chapter recording and all 24 distinct hashed cues; playback/mixed-signal, ducking, warning capacity, mute, pause, context interruption and single-play defeat-cue checks run across desktop Chromium, Android-sized Chromium and iPhone-sized WebKit. The unchanged menu recording still passes its playback/mute/scene-switch check. The staged production-mixer review recording is `artifacts/audio-review-0.31.mp3`; it is for human listening, not evidence that a person judged the mix or musical edits.
- The broader 0.30 browser run and final rendering/save rerun remain applicable to unchanged gameplay: 214 passed and 14 skipped in the broad run, six stale scope/count assumptions were corrected, then 50 focused checks passed with four skips after final sprite clipping. The entire broad suite was not rerun for 0.31's soundtrack replacement. [Exact prior-pass results](VALIDATION-0.30.md).
- TypeScript/Vite build and Android/iOS Capacitor sync pass. Android `assembleDebug bundleRelease lintRelease` passes in 14 seconds, version **0.31.0 / code 31**. Main JavaScript is **389.45 kB / 142.94 kB gzip**; CSS **54.32 kB / 12.38 kB gzip**. iOS project syntax, Info.plist and PrivacyInfo.xcprivacy parse. Native iOS compilation/signing remains unverified on Linux.
- The final APK was installed on the dedicated Android API 36 emulator. **All 12 native checks pass with zero page errors**: landscape launch, **menu transport advancing before any tap on cold launch**, original/new hero and enemy assets decoding offline, all ten selected Udio recordings available offline with matching hashes, native preferences, Jump/Power touches, Back pause, Home/foreground pause, process-death profile/bookmark retention, and Continue without duplicate banked rewards. [Native report](ANDROID-SMOKE-0.31.0.json). Home and resumed-game captures were inspected. The emulator has no audio output; cold-launch transport is not human listening evidence. Physical-device performance and broader native campaign/lifecycle coverage remain open.

- Packaging verifies the exact current production web bytes inside both APK and AAB. The Android debug APK is **50.55 MiB** and the unsigned release bundle **49.12 MiB**; APK, AAB and source-archive SHA256SUMS checks pass. The release bundle still requires signing before store submission.

The packaged game includes the 0.30 attack/enemy art, blood, Heavy-spam counters and eighteen extra districts as well as the new soundtrack. The full game-quality target remains open: human enjoyment, more exploit searches, pacing, more fluid animation, sustained co-op, cue/loop listening and physical Android/native iOS release readiness still need work. [Quality target](QUALITY-TARGET.md), [previous released validation](VALIDATION-0.29.md).
