# Local validation — 0.17.0, 2026-09-08

- **158 simulation tests passed** across 17 files, including all nine earned-progression hero/style campaigns and cooperative chapter play. After adding explicit early-versus-late jump checks, all seven support-enemy tests passed again.
- The full browser run passed **127 checks**, with eight inapplicable combinations skipped, across desktop Chromium, Android-sized Chromium and iPhone-sized WebKit. Three isolated-jelly slap checks were then added. After strengthening the slap warning's contrast, **all six support-enemy browser checks passed** on the final rendering code. That is 130 distinct passing browser combinations across the full and focused runs. Host-requirement validation was skipped by environment flag; actual browser execution completed.
- Real-input laundry tests capture a naturally occurring stitch, then a damaging interruption. Separate, explicitly staged isolated-jelly fixtures use real keyboard jump inputs and the production damage path. They verify a late jump avoids the slap; the unit test also verifies an immediate jump can land too early. These are mechanics and integration checks, not human enjoyment measurements.
- Final iPhone-sized stitch, interruption and slap screenshots were visually inspected. The first slap outline lacked contrast on sand; it now has a dark under-stroke, moving cream dashes and an outlined “SLAP!” label. Healing retains a distinct green seam, traveling stitches and patient progress ring. Captures: `artifacts/triage-{stitch,cut,slap}-{desktop,android,iphone}.png`.
- The **432-run combat audit completed with no timeouts**, using 0.14's earned profiles and seeds. Light wins 0/108; Heavy 31/108, including 17/54 boss runs; Light + power 24/108; mixed 106/108, including 53/54 boss runs. The mixed steering now prioritizes visible stitches, so its comparison includes a probe change. Heavy-only total wins are unchanged. See [raw audit](COMBAT-AUDIT-0.17.json) and [interpretation](UPDATE-0.17.md).
- Strict TypeScript, final production Vite build and Capacitor Android/iOS sync passed. Main JavaScript: **320.86 kB / 115.76 kB gzip**; CSS: **53.81 kB / 12.24 kB gzip**. Rollup removes misplaced pure-annotation comments in the Zod dependency; the build succeeds.
- Android `assembleDebug bundleRelease lintRelease` passed again with the final warning contrast assets. Version **0.17.0/code 17** was independently confirmed from the APK manifest. The debug APK is installable; the release AAB remains unsigned. The existing dependency lint baseline remains.
- Xcode project syntax and two iOS plists parse. Native iOS compilation, signing and device behavior remain unverified on this Linux host.
- Packaging compares every current web/audio asset in both native packages before collecting downloads and generating checksums for the APK, unsigned AAB and source archive.

The goal remains open: this pass deepens one enemy role and its feedback. It does not establish reference-game quality or resolve the broader Heavy-only exploit. Journey pacing, human play/listening, physical controllers/device lifecycle/performance and native iOS release work remain outstanding. See [quality target](QUALITY-TARGET.md), [playtest kit](PLAYTEST.md), [0.17 changes](UPDATE-0.17.md), and [previous validation](VALIDATION-0.16.md).
