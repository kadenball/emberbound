# Local validation — 0.13.0, 2026-09-08

- **142 simulation tests passed** in 15 files. Eleven new counter cases cover grounded versus overhead hits, lane escape, committed high burps, alternating Candy volleys and fork heights, legacy hazard interpretation, and one-time impact damage/rewards. Existing nine hero/style campaign probes still complete using earned progression and validate bookmarks at every fight entry and cleared road.
- **94 browser checks passed**, eight inapplicable combinations skipped, across desktop Chromium, Android-sized Chromium and iPhone-sized WebKit. After the full run, the forklift warning was extended to its exact arena travel bounds using the same boundary helper as movement; all **12 boss-counter browser checks passed again** on that final build. All 142 simulation tests also passed again. Browser host-requirement validation was skipped by environment flag; actual Chromium/WebKit execution completed.
- New browser tests play four boss chapters using normal accelerated inputs from previously earned profiles, hold each new warning and its subsequent impact, assert the overhead instruction and combo HUD bounds, and capture both states. No health/position edits or skipped waves are used. Existing save/resume, first-boss and final-ending UI coverage passed in the full run.
- Phone-sized jaw, fork and stamp captures were visually inspected. The large combo overlay obscured the boss in the initial captures; it was moved to the upper HUD. The revised jaw warning and Android ice impact were inspected again. Capture paths are in [0.13](UPDATE-0.13.md).
- **432 matched-profile combat runs** completed with no timeouts. Heavy-only won 43/108 chapters and 26/54 boss chapters, including all nine Candy and all nine Frank samples. Mixed play won 107/108 chapters. The audit does not show an overall difficulty increase; it exposes a remaining exploit. The mixed policy responds to new visible counters, so the two versions are not identical input recordings. See the [before/after methodology](UPDATE-0.13.md).
- Strict TypeScript, production Vite build and Capacitor Android/iOS sync passed. The main production script is 295.53 kB / 105.82 kB gzip. Rollup removes two misplaced pure-annotation comments in the Zod dependency; the build succeeds.
- Android `assembleDebug bundleRelease lintRelease` passed on the final assets, **version 0.13.0/code 13**. The debug APK is installable; the release AAB remains unsigned. The existing dependency lint baseline remains.
- Xcode project syntax and two iOS plists parse. Native iOS build/signing has not been verified on this Linux host.
- Packaging compares every web/audio asset in both native packages with the current production build before writing downloadable packages and source checksums.

The full quality target remains open. The counters now agree with their animations, but late-game Heavy-spam wins, repeated arena scenery, authored travel pacing, physical-device testing, native iOS release work, gamepad menu navigation and human music/enjoyment review remain outstanding. See [the open target](QUALITY-TARGET.md), [0.13 changes](UPDATE-0.13.md), [playtest kit](PLAYTEST.md) and [prior validation](VALIDATION-0.12.md).
